#define forever for(;;) /* infinite loop */
#define min(A, B) ((A) < (B) ? (A) : (B))
#define max(A, B) ((A) > (B) ? (A) : (B))
#define daysofmonth[] {31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31}
#define dprint(expr) printf(#expr " = %g\n", expr)
#define enum boolean { NO, YES };
#define enum escapes { BELL = '\a', BACKSPACE = '\b', TAB = '\t', NEWLINE = '\n', VTAB = '\v', RETURN = '\r' };
#define enum months { JAN = 1, FEB, MAR, APR, MAY, JUN, JUL, AUG, SEP, OCT, NOV, DEC };